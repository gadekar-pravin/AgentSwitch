# Secrets. Only .env.example is committed.
.env
.env.*
!.env.example

# Python and tooling
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store
.idea/

# Harness run records. Kept on disk as scoring evidence, not committed:
# they hold live tenant data. Root-anchored so a future docs/runs/ is not swallowed.
/runs/

# Raw dumps from the live platform (schemas, tools/list, records).
/dumps/

# Course brief: kept locally only, not published. assignment-overview.md is our public summary.
/agentswitch-team-brief.md

# Personal Claude Code instructions. Team rules live in CLAUDE.md.
CLAUDE.local.md
# Local symlink to CLAUDE.md so Codex reads the same team rules (ln -s CLAUDE.md AGENTS.md)
AGENTS.md

# Private visual explainer published as a claude.ai Artifact. Kept local so Codex and Claude can
# edit it; not committed because the page is shared privately, not through this public repo.
/explainer/

# Superseded drafts kept locally for reference, not committed.
/archive/
