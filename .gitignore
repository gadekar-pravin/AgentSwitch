# Secrets. Only .env.example is committed.
.env
.env.*
!.env.example

# Python and tooling
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.DS_Store
.idea/

# Harness run records. Kept on disk as scoring evidence, not committed:
# they hold live tenant data. Root-anchored so a future docs/runs/ is not swallowed.
/runs/

# Raw dumps from the live platform (schemas, tools/list, records).
/dumps/

# Course brief: kept locally only. Each teammate places their own copy at the repo root.
/agentswitch-team-brief.md

# Personal Claude Code instructions. Team rules live in CLAUDE.md.
CLAUDE.local.md
